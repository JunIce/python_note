#!/bin/bash
#encoding=utf-8
'''
  Tuple 是不可变的 list。一旦创建了一个 tuple，就不能以任何方式改变它。
  Tuple 没有方法
  Tuples 可以在 dictionary 中被用做 key
  Tuple 本身是不可改变的，但是如果您有一个 list 的 tuple，那就认 为是可变的了，用做 dictionary key 就是不安全的。只有字符串、整数 或其它对 dictionary 安全的 tuple 才可以用作 dictionary key。
  Tuple不可变 ， 因此没有操作方法
  in 检查是否在Tuple内
'''